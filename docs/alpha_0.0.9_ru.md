# 🚀 Документация Движка GNU OpenGL Engine (PEngen)

Привет\! Рад представить тебе полную документацию для **GNU OpenGL Engine**, который я ласково называю **PEngen**\!

| Параметр | Значение |
| :--- | :--- |
| **Имя Движка** | **GNU OpenGL Engine (PEngen)** |
| **Версия** | **0.0.9a** |
| **Основа** | C++, OpenGL (GLFW/GLAD), GLM |
| **Цель Создания** | Этот движок был создан для того, чтобы разработчики могли **быстро и с удовольствием** создавать работающие 3D-сцены, не утопая в рутине настройки OpenGL. PEngen предоставляет готовые инструменты для загрузки моделей, управления камерой (в стиле FPS), освещением, а также включает простой, но функциональный 2D-интерфейс (UI) и кроссплатформенный (почти) фреймворк для ввода и аудио\!

Готовься погрузиться в мир 3D-графики\!

<img src="sccren1.jpg" alt="Снимок экрана PEngine Demo" width="800"/>

-----

## 🚀 1. Модуль Ядра (`Engine` и `Scene`)

### Структура `PEngine::Vec` (Математика)

| Структура | Поля | Описание |
| --- | --- | --- |
| **`Vec2`** | `float x, y` | Координаты для 2D/UI. |
| **`Vec3`** | `float x, y, z` | Координаты для 3D пространства и физики. |

### Класс `PEngine::Engine`

Центральный класс, управляющий окном и жизненным циклом приложения.

* **`Engine(int width, int height, const std::string& title)`**: Инициализирует GLFW, создает окно и настраивает контекст.
* **`bool ShouldClose()`**: Проверяет, не закрыл ли пользователь окно.
* **`void Update()`**: Очищает буферы, обновляет ввод и меняет кадры. Вызывать в конце каждого цикла.
* **`Scene* GetScene()`**: Дает доступ к объектам мира.
* **`~Engine()`**: **(Удаление)** Закрывает окно и освобождает ресурсы системы.

### Класс `PEngine::Scene` и структура `Object`

Объект сцены (`Object`) — это контейнер для визуализации.

**Поля `Object`:**

* `long long id`: Уникальный ID.
* `std::string name`: Имя для поиска.
* `Vec3 pos, size, rotator`: Трансформация.
* `uint8_t state`: Тип (1: Куб, 2: Модель, 3: Кнопка, 4: Чекбокс, 5: Ввод, 6: Панель, 7: Картинка).
* `PEGLMaterial material`: Цвета и текстуры.
* `PEGLModel model`: Данные 3D сетки.
* `void (*on_click)()`: Функция-событие при нажатии.

**Функции `Scene`:**

* **`AddObject(const Object& object)`**: Добавляет объект в мир.
* **`SearchObject(const char* name)`**: Ищет объект по имени.
* **`GetObjectById(long long id)`**: Ищет объект по ID.
* **`SetCamera(Vec3 position, float yaw, float pitch, float roll)`**: Управляет видом игрока.
* **`UpdateUI(GLFWwindow* window)`**: Обрабатывает интерактив в UI.
* **`Render()`**: Рисует всё содержимое сцены.

---

## ⚛️ 2. Модуль Физики (`Physics.h`, `Physics_Base.h`)

Физика работает параллельно со сценой, обновляя координаты визуальных объектов.

### Структура `PhysicsObject`

* `long long linked_object_id`: **Связь!** ID визуального объекта из `Scene`.
* `Vec3 pos, size, velocity, speed_gravity`: Параметры движения и размеров.
* `float mass`: Масса объекта.
* `uint8_t colision_type`: `0` - нет, `1` - триггер, `2` - твердое тело (отскок).
* `int friction`: Сила трения.
* `void (*on_collision)(PhysicsObject& other)`: Событие при столкновении.

### Класс `PEngine::Physics_Base`

* **`Physics_Base(Scene* scene)`**: Конструктор, связывающий физику со сценой.
* **`AddPhysicsObject(const PhysicsObject& physics_object)`**: Регистрирует тело в симуляции.
* **`SearchPhysicsObject(const char* name)`**: Находит тело по имени.
* **`UpdatePhysics(float deltaTime)`**: Проводит расчет столкновений и перемещений.

---

## 🗑️ 3. Модуль Очистки и Удаления (`OpenGL.h`)

Функции для освобождения памяти видеокарты (GPU).

* **`void PEGLDelete_Model(PEGLModel& model)`**: Удаляет меши, VAO, VBO и EBO. Вызывать, когда 3D модель больше не нужна.
* **`void PEGLDelete_Shader_Program(PEGLShaderProgram& program)`**: Удаляет скомпилированный шейдер.
* **`void PEAStopAll()`**: (из Audio) Останавливает все звуки и закрывает их дескрипторы.

---

## 🔊 4. Модуль Аудио (`Audio.h`)

Класс `AudioManager` (все функции статические).

* **`PEAPlaySound(const std::string& filePath, int id)`**: Одиночное воспроизведение.
* **`PEAPlayLoopedSound(const std::string& filePath, int id)`**: Фоновая музыка (цикл).
* **`PEAStopSound(int id)`**: Остановка конкретного звука по ID.
* **`PEASetVolume(float volume, int id)`**: Установка громкости (0.0 - 1.0).

---

## ⌨️ 5. Модуль Ввода (`Input.h`)

Класс `InputManager` для эмуляции и управления.

* **`PEIKeyPress(WORD vkCode)`**: Нажать и отпустить клавишу.
* **`PEIKeyDown(WORD vkCode)`**: Зажать клавишу.
* **`PEIKeyUp(WORD vkCode)`**: Отпустить клавишу.
* **`PEIMouseClickL()` / `PEIMouseClickR()**`: Клики мыши.
* **`PEIMouseMoveAbsolute(int x, int y)`**: Переместить курсор в точку экрана.

---

## 💾 6. Модуль Конфигов и Логов (`cfg.h`, `log.h`)

* **`void PEsave(const string& filePath, const string& data)`**: Сохраняет текст в папку `PEngine\cfg\`.
* **`string PEload(const string& filePath)`**: Загружает текст из файла.
* **`void PElogger(const char* message)`**: Записывает важное событие в `log.txt` с датой.
* **`void PElogger_up(const char* message)`**: Записывает частые обновления в `log_update.txt`.

---

## 🎨 7. Низкоуровневая Графика (`gnu` namespace)

* **`GLuint PEGLLoad_Texture_From_File(const std::string& filePath)`**: Загрузка текстуры.
* **`PEGLModel PEGLLoad_Model_From_File_OBJ(const std::string& path, const std::string& dir)`**: Загрузка .obj модели.
* **`void PEGLprint_string(float x, float y, const char* text, float r, float g, float b, float scale, bool flip_y)`**: Отрисовка текста.